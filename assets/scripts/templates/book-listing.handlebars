<div class="books">
  {{#each books as |book|}}
  <div class="book">
    <h2>
      <small>Title:</small> {{limit book.title 40}}
    </h2>
    <p>
      <small>Author:</small>
      {{#if book.author}}
        {{book.author}}
      {{else}}
        No author provided.
      {{/if}}
    </p>
  </div>
</div>

{{/each}}
